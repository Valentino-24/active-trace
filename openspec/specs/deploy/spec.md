# Deploy y CI (C-24)

### AT-01: Dockerfile multi-stage para frontend (Vite build → nginx)
### AT-02: Servicio frontend en docker-compose.yml
### AT-03: nginx.conf con proxy reverso a /api/
### AT-04: GitHub Actions CI: pytest (backend) + vitest (frontend) + build
### AT-05: docker compose up levanta todos los servicios
